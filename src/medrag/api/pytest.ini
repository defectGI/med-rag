[pytest]
# Collection is pinned to `tests` so argument-less `python -m pytest` never
# sweeps in other `tests/` packages (e.g. separately installed vendored
# engines). Without this, two packages named `tests` collide and the run dies
# with "ModuleNotFoundError: No module named 'tests.conftest'".
testpaths = tests
