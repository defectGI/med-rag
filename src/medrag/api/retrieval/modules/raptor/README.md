# raptor

Status: **planned, not started.**

RAPTOR-style recursive abstractive tree retrieval (hierarchical
summarization + retrieval over the tree). Design and interface TBD.

Extra: `pip install -e ".[raptor]"`
