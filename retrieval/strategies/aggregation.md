# Strategy: aggregation

**Intent:** `aggregation`
**What it means:** counting, filtering, listing, or an extreme value of ANY attribute (price, weight, temperature range, power, speed, ...) across MANY products.

> Advisory / guidance prompt only — no auto tool-calling (see [README](./README.md)).

## Prompt draft

_(empty — to be written when this intent's strategy is worked on)_

## Notes

- Likely sources (non-binding): `product_nodes.json` (structured / db_query).
-
